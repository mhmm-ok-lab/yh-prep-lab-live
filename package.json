{
  "name": "yh-study-portal",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc -b && vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "verify": "npm run test && npm run build",
    "audit:visual": "node playwright-visual-audit.mjs"
  },
  "devDependencies": {
    "playwright": "^1.59.1",
    "typescript": "^5.9.3",
    "vite": "^7.1.11",
    "vitest": "^3.2.4"
  }
}
